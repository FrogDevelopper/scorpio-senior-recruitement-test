# How to

## compile
```shell
./gradlew classes
```

## Build the application
```shell
./gradlew package
```

## Run the application
- the main function is in the `PlcApplication` class. Run the main function with your favourite IDE.
- or run
    ```shell
    ./gradlew run
    ```

## Run the tests
```shell
./gradlew test
```
